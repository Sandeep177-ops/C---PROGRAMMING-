#include <stdio.h>
void main()
{
    int i;
    scanf("%d",&i);
    while(i<=10)
    {
        if(i%2==1)
        {
            
        
        printf("%d the number is odd\n",i);
        } 
        
        i++;
    }
    printf("the number is even");
}
