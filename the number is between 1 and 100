
#include <stdio.h>

int main()
{
    int num;
    scanf("%d",&num);
    if(num>=1 && num<=100)    
    {
        printf("the number is between the range");   
    }
    else
    {
        printf("out of range");
    }
    
    return 0;
}
