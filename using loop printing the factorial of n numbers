#include <stdio.h>
void main()
{
    int i=1,n,sum=1;
    printf("enter the number:");
    scanf("%d",&n);
    while(i<=n)
    {
        sum=sum*i;
        i++;
    }
    printf("the factorial is:%d",sum);
    
}
